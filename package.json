{
  "name": "zotero-translation-client",
  "license": "AGPL-3.0",
  "version": "1.0.0",
  "description": "",
  "repository": "",
  "main": "lib/main.js",
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "files": [
    "lib",
    "build"
  ],
  "config": {
    "port": "8001",
    "interface": "127.0.0.1",
    "translation_server": "http://localhost:1969"
  },
  "scripts": {
    "prepack": "npm run build",
    "postpack": "rimraf build",
    "browserify": "mkdir -p build/ && NODE_ENV=production browserify -s ZoteroTranslationClient -d src/js/main.js -t [babelify] -g [ envify --NODE_ENV production ] -g uglifyify | uglifyjs --compress --mangle > build/zotero-translation-client.js",
    "build": "rimraf build && run-p browserify",
    "clean": "rimraf build",
    "test": "NODE_ENV=test nyc mocha",
    "coverage": "NODE_ENV=test nyc --reporter=html --reporter=text mocha",
    "start": "NODE_ENV=test mocha --watch"
  },
  "dependencies": {
    "babel-regenerator-runtime": "^6.5.0",
    "isomorphic-fetch": "^2.2.1",
    "parse-link-header": "^1.0.1",
    "zotero-base-mappings": "0.0.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-async-to-generator": "^6.24.1",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-plugin-transform-regenerator": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "babel-runtime": "^6.26.0",
    "babelify": "^7.3.0",
    "browserify": "^14.4.0",
    "chai": "^4.1.2",
    "citeproc": "^2.1.180",
    "envify": "^4.1.0",
    "fetch-mock": "^5.12.2",
    "mocha": "^3.5.3",
    "npm-run-all": "^4.1.1",
    "nyc": "^11.2.1",
    "rimraf": "^2.6.2",
    "sinon": "^4.0.0",
    "uglify-js": "^3.3.26",
    "uglifyify": "^5.0.0",
    "watchify": "^3.9.0"
  }
}
